# Copy to .env and override for local/production.
POSTGRES_DB=robbo_projects
POSTGRES_USER=robbo_projects
POSTGRES_PASSWORD=robbo_projects_change_me
# Host port (container listens on 5432 internally)
ROBBO_PROJECTS_DB_PORT=5433
